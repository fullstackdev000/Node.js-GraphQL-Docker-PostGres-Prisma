enum AccountExtensionType {
  BASIC, PRO
}

enum ActivityStatus {
  ACTIVE, SUSPENDED
}

enum Appearance { Always, Branded, Hide, Unbranded }

enum AreaMeasurementUnit {
  Acres, Hectares, SquareFeet, SquareMeters
}

enum BillingFrequency {
  BIWEEKLY, DAILY, MONTHLY, WEEKLY
}

enum Country {
  AF, AX, AL, DZ, AS, AD, AO, AI, AQ, AG, AR, AM, AW, AU, AT, AZ, BS, BH, BD, BB, BY, BE, BZ, BJ, BM, BT, BO, BQ, BA, BW, BV, BR, IO, BN, BG, BF, BI, KH, CM, CA, CV, KY, CF, TD, CL, CN, CX, CC, CO, KM, CG, CD, CK, CR, CI, HR, CU, CW, CY, CZ, DK, DJ, DM, DO, EC, EG, SV, GQ, ER, EE, ET, FK, FO, FJ, FI, FR, GF, PF, TF, GA, GM, GE, DE, GH, GI, GR, GL, GD, GP, GU, GT, GG, GN, GW, GY, HT, HM, VA, HN, HK, HU, IS, IN, ID, IR, IQ, IE, IM, IL, IT, JM, JP, JE, JO, KZ, KE, KI, KP, KR, KW, KG, LA, LV, LB, LS, LR, LY, LI, LT, LU, MO, MK, MG, MW, MY, MV, ML, MT, MH, MQ, MR, MU, YT, MX, FM, MD, MC, MN, ME, MS, MA, MZ, MM, NA, NR, NP, NL, NC, NZ, NI, NE, NG, NU, NF, MP, NO, OM, PK, PW, PS, PA, PG, PY, PE, PH, PN, PL, PT, PR, QA, RE, RO, RU, RW, BL, SH, KN, LC, MF, PM, VC, WS, SM, ST, SA, SN, RS, SC, SL, SG, SX, SK, SI, SB, SO, ZA, GS, SS, ES, LK, SD, SR, SJ, SZ, SE, CH, SY, TW, TJ, TZ, TH, TL, TG, TK, TO, TT, TN, TR, TM, TC, TV, UG, UA, AE, GB, US, UM, UY, UZ, VU, VE, VN, VG, VI, WF, EH, YE, ZM, ZW
}

enum CreditCardName {
  AMERICANEXPRESS, MASTERCARD, VISA
}

enum Currency {
  CAD, GBP, MXP, USD
}

enum DomainProvider { AFFILIATE, NAMECHEAP }

enum DurationUnit {
  Hour
  Minute
}

enum FlyerLayoutName {
  FEATURED1, FEATURED1MINOR5, FEATURED2MINOR3, FEATURED2MINOR6, FEATURED2MINOR6WITHHORIZONTALBARS
}

enum MediaInteractiveType {
  EMBEDDED
  FLOORPLAN_PHOTOS
}

enum ListingType {
  SingleFamilyHome
  Townhouse
  Cooperative
  RentalLease
  LotLand
  Commercial
  Cottage
  Industrial
  ResidentialHome
}

enum OrderedServiceStatus {
  Completed
  Pending
}

enum PanoramaType {
  CYLINDRICAL
  SPHERICAL
}

enum PaymentProcessor {
  PAYPAL, STRIPE
}

enum CollectPayment {
  ORDER, DELIVERY
}

enum PhotoDownloadTriggers {
  NOTRIGGER, UPONACTIVATION
}

enum RealEstateSiteMediaStyles {
  MODERN, SLIDESHOW, RESPONSIVE, VIDEO
}

enum RealEstateStatus {
  PENDING, SOLD
}

enum TimePeriod { Day, Week, Month }

enum Role { ADMIN, AFFILIATE, AGENT, DEVELOPER, PHOTOGRAPHER, PROCESSOR, SELFSERVICEAGENT }

enum ServiceCategoryIcon {
  Aerial
  FloorPlan
  Landscape
  Panorama
  Photo
  Print
  Other
  SocialMedia
  Video
  Vr3D
}

enum ServiceType {
  Primary
  AddOn
  Admin
  Upgrade
}

enum State {
  AL, AK, AZ, AR, CA, CO, CT, DE, DC, FL, GA, HI, ID, IL, IN, IA, KS, KY, LA, ME, MD, MA, MI, MN, MS, MO, MT, NE, NV, NH, NJ, NM, NY, NC, ND, OH, OK, OR, PA, RI, SC, SD, TN, TX, UT, VT, VA, WA, WV, WI, WY
}

enum VideoCategory {
  Properties
  Agents
  Flyover
  Developments
  Neighborhoods
  MarketReports
}

enum VideoStatus {
  INITIAL
  IN_PROGRESS
  ERROR
  OK
}

enum VideoType {
  Faux
  Hosted
  Embed
  URL
}

enum GenerateOption {
  Branded
  Unbranded
  Package
}

enum SlideTransition {
  Crossfade
  Fade
  Slide
}
enum ZippingStatus {
  InProgress
  Ready
}